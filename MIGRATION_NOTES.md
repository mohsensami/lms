# migrate: MongoDB/Mongoose → PostgreSQL (Neon) + Prisma

## چیکار شد؟

- `mongoose` به طور کامل حذف شد و به جاش `@prisma/client` + `prisma` اضافه شد.
- فایل‌های `model/*.js` (اسکیمای Mongoose) و `service/mongo.js` حذف شدند.
- اسکیمای Prisma در `prisma/schema.prisma` ساخته شد که معادل دقیق همون مدل‌های قبلی مونگو هست.
- کلاینت Prisma به صورت singleton در `lib/prisma.js` ساخته شد.
- همه‌ی `queries/*.js` و `app/actions/*.js` و مسیرهای API (`app/api/...`) و `auth.js` بازنویسی شدند تا از Prisma استفاده کنن به‌جای Mongoose، بدون تغییر در امضای توابع (اسم تابع و ورودی‌هاش دقیقاً همونیه که کل بقیه‌ی پروژه صداش می‌زنه).
- `.env` با اطلاعات Neon که فرستادی آپدیت شد (`DATABASE_URL` و `DATABASE_URL_UNPOOLED`).

## آپدیت: رفع خطای اعتبارسنجی اسکیما (`_id` نامعتبر بود)

توی تلاش اول، فیلد آی‌دی رو مستقیم `_id` گذاشته بودم که Prisma قبولش نمی‌کنه (اسم فیلد نمی‌تونه با `_` شروع بشه — دقیقاً همون ارور `P1012`ای که گرفتی). الان درستش کردم:

- توی `prisma/schema.prisma` اسم فیلد آی‌دی همه‌ی مدل‌ها استاندارد شد: `id String @id @default(cuid())`.
- توی `lib/prisma.js` یه **Prisma Client Extension** اضافه کردم که خروجی هر کوئری رو (recursively، شامل رابطه‌های `include` شده و آرایه‌ها) می‌گرده و کلید `id` رو به `_id` تغییر نام می‌ده. یعنی نتیجه‌ای که به بقیه‌ی کد می‌رسه دقیقاً همون شکل قبلیه (`course._id`، `module._id`، …) و **هیچ کدوم از کوئری‌ها/اکشن‌ها/کامپوننت‌های UI لازم نیست دوباره دست بخوره**.
- توی همه‌ی `queries/*.js` و `app/actions/*.js` و مسیرهای API، جاهایی که مستقیم داشتم با `where: { _id: ... }` یا `select: { _id: true }` به Prisma کوئری می‌زدم رو به `where: { id: ... }` / `select: { id: true }` اصلاح کردم (چون این‌ها آرگومان ورودی به Prisma هستن، نه خروجی — فقط خروجی‌هاست که extension تبدیلش می‌کنه).

## قبل از اجرا حتماً این مراحل رو انجام بده

چون این‌جا (محیط من) به اینترنت دسترسی ندارم، نتونستم `npm install` و `prisma generate` رو خودم اجرا کنم و تست کنم. پس روی سیستم خودت این‌ها رو اجرا کن:

```bash
npm install
npx prisma generate
npx prisma db push
npm run dev
```

- `npm install` → پکیج‌های جدید (`@prisma/client`, `prisma`) رو نصب می‌کنه (و `mongoose` دیگه توی `package.json` نیست).
- `npx prisma generate` → کلاینت Prisma رو بر اساس اسکیما می‌سازه (این به صورت خودکار بعد از `npm install` هم اجرا می‌شه چون `postinstall` تنظیم شده).
- `npx prisma db push` → جدول‌ها رو مستقیم روی دیتابیس Neon می‌سازه (چون دیتابیس خالیه و migration history لازم نداری؛ اگه بعداً خواستی migration باقاعده داشته باشی می‌تونی از `npx prisma migrate dev` استفاده کنی).

## چیزهایی که تغییر کردن ولی رفتارشون یکیه

- جاهایی که قبلاً آرایه‌ی id توی مدل والد نگه‌داری می‌شد (مثلاً `Course.modules`، `Module.lessonIds`، `Quizset.quizIds`) الان با فارین‌کی واقعی (`courseId`، `moduleId`، `quizsetId`) پیاده‌سازی شدن؛ یعنی وقتی یه ماژول/لسون/کوییز می‌سازی، رابطه‌ش مستقیم موقع create ثبت می‌شه، نه با push کردن به آرایه.
- حذف کردن یه دوره الان به‌صورت خودکار ماژول‌ها، تستیمونیال‌ها، انرولمنت‌ها و ریپورت‌های وابسته‌شو هم پاک می‌کنه (`onDelete: Cascade`)، چون توی Postgres برخلاف Mongo نمی‌شه یه رکورد رو با فارین‌کی وابسته حذف کرد بدون این تنظیم.
- منطق تشخیص «تکمیل شدن ماژول» توی `queries/reports.js` که قبلاً با آرایه‌ی ObjectId (که واقعاً درست کار نمی‌کرد چون مقایسه‌ی رفرنس آبجکت بود نه مقدار) بود، الان با `Set` روی رشته‌ها انجام می‌شه که درست‌تر و قابل‌اعتمادتره.

## چیزهایی که دست نخوردن

- هیچ کامپوننت UI، هیچ صفحه، استایل، فرم یا منطق نمایش تغییر نکرده — فقط لایه‌ی دیتابیس عوض شده.
- Stripe و Zarinpal دست نخوردن (اصلاً به دیتابیس مستقیم دسترسی نداشتن).
